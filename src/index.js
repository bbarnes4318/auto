import React from 'react';
import ReactDOM from 'react-dom/client';
import FinancialDashboard from './auto.jsx';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <FinancialDashboard />
  </React.StrictMode>
);
